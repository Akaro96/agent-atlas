# __WORKSPACE_NAME__

This is an umbrella workspace.

## Rules

- move into the real target project before heavy work
- keep the root clean
- if a child folder becomes a real repo, add repo-local `AGENTS.md`
- keep deeper docs in project-local `docs/`
- keep durable knowledge in the separate vault
- do not mirror every new project into the vault automatically
- if a project becomes serious and still has no vault coverage, create an inbox draft first via `_SHARED/tools/Sync-WorkspaceProjectDrafts.ps1`
- do not auto-write directly into curated vault folders without deliberate review
